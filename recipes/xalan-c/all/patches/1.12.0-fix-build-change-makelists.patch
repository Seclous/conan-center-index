diff --git a/CMakeLists.txt b/CMakeLists.txt
index 2c5eea1a7..81129ad43 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -19,29 +19,33 @@
 
 # Run "cmake" to generate the build files for your platform
 
-cmake_minimum_required(VERSION 3.2.0)
+cmake_minimum_required(VERSION 3.24)
+
+option(XALANC_BUILD_TESTS    "Build unit tests" ON)
+option(XALANC_BUILD_EXAMPLES "Build examples" OFF)
+option(XALANC_BUILD_DOCS     "Build Doxygen documentation" OFF)
 
 # Use new variable expansion policy.
 if (POLICY CMP0053)
-  cmake_policy(SET CMP0053 NEW)
-endif(POLICY CMP0053)
+    cmake_policy(SET CMP0053 NEW)
+endif (POLICY CMP0053)
 if (POLICY CMP0054)
-  cmake_policy(SET CMP0054 NEW)
-endif(POLICY CMP0054)
+    cmake_policy(SET CMP0054 NEW)
+endif (POLICY CMP0054)
 if (POLICY CMP0067)
-  cmake_policy(SET CMP0067 NEW)
-endif(POLICY CMP0067)
+    cmake_policy(SET CMP0067 NEW)
+endif (POLICY CMP0067)
 
 # Try C++14, then fall back to C++11 and C++98.  Used for feature tests
 # for optional features.
-set(CMAKE_CXX_STANDARD 14)
+set(CMAKE_CXX_STANDARD 20)
 
 # Use folders (for IDE project grouping)
 set_property(GLOBAL PROPERTY USE_FOLDERS ON)
 
 # Source additional modules from the "cmake" directory
 list(APPEND CMAKE_MODULE_PATH
-     "${CMAKE_CURRENT_LIST_DIR}/cmake")
+        "${CMAKE_CURRENT_LIST_DIR}/cmake")
 
 # Project version
 project(xalan-c
@@ -58,7 +62,9 @@ message(STATUS "Configuring Apache Xalan-C++ version ${xalan-c_VERSION}")
 
 enable_testing()
 find_package(Threads REQUIRED)
-include(Doxygen)
+if (XALANC_BUILD_DOCS)
+    include(Doxygen)
+endif ()
 include(GNUInstallDirs)
 include(XalanDebug)
 include(XalanDLL)
@@ -85,11 +91,11 @@ set(exec_prefix "${CMAKE_INSTALL_PREFIX}")
 set(libdir "${CMAKE_INSTALL_FULL_LIBDIR}")
 set(includedir "${CMAKE_INSTALL_FULL_INCLUDEDIR}")
 configure_file(${CMAKE_CURRENT_SOURCE_DIR}/xalan-c.pc.in
-               ${CMAKE_CURRENT_BINARY_DIR}/xalan-c.pc)
+        ${CMAKE_CURRENT_BINARY_DIR}/xalan-c.pc)
 install(
-  FILES ${CMAKE_CURRENT_BINARY_DIR}/xalan-c.pc
-  DESTINATION "${PKGCONFIGDIR}"
-  COMPONENT "development")
+        FILES ${CMAKE_CURRENT_BINARY_DIR}/xalan-c.pc
+        DESTINATION "${PKGCONFIGDIR}"
+        COMPONENT "development")
 
 # Process subdirectories
 add_subdirectory(src/xalanc/Utils/MsgCreator)
@@ -98,9 +104,15 @@ add_subdirectory(src/xalanc/Utils/XalanMsgLib)
 add_subdirectory(src/xalanc)
 add_subdirectory(src/xalanc/TestXSLT)
 add_subdirectory(src/xalanc/TestXPath)
-add_subdirectory(samples)
-add_subdirectory(Tests)
-add_subdirectory(docs/doxygen)
+if (XALANC_BUILD_EXAMPLES)
+    add_subdirectory(samples)
+endif()
+if (XALANC_BUILD_TESTS)
+    add_subdirectory(Tests)
+endif()
+if (XALANC_BUILD_DOCS)
+    add_subdirectory(docs/doxygen)
+endif()
 
 # Display configuration summary
 message(STATUS "")
@@ -108,19 +120,22 @@ message(STATUS "Xalan-C++ configuration summary")
 message(STATUS "-------------------------------")
 message(STATUS "")
 message(STATUS "  Version:                     ${xalan-c_VERSION}")
-if(MSVC)
-message(STATUS "  Library major version:       ${XALAN_MS_LIB_MAJOR_VER}")
-message(STATUS "  Library minor version:       ${XALAN_MS_LIB_MINOR_VER}")
-else()
-message(STATUS "  Library major version:       ${XALAN_LIB_MAJOR_VER}")
-message(STATUS "  Library minor version:       ${XALAN_LIB_MINOR_VER}")
-endif()
+if (MSVC)
+    message(STATUS "  Library major version:       ${XALAN_MS_LIB_MAJOR_VER}")
+    message(STATUS "  Library minor version:       ${XALAN_MS_LIB_MINOR_VER}")
+else ()
+    message(STATUS "  Library major version:       ${XALAN_LIB_MAJOR_VER}")
+    message(STATUS "  Library minor version:       ${XALAN_LIB_MINOR_VER}")
+endif ()
 message(STATUS "")
 message(STATUS "  Installation directory:      ${prefix}")
 message(STATUS "  C compiler:                  ${CMAKE_C_COMPILER}")
 message(STATUS "  C++ compiler:                ${CMAKE_CXX_COMPILER}")
 message(STATUS "")
 message(STATUS "  Build shared libraries:      ${BUILD_SHARED_LIBS}")
+message(STATUS "  Build tests:                 ${XALANC_BUILD_TESTS}")
+message(STATUS "  Build examples:              ${XALANC_BUILD_EXAMPLES}")
+message(STATUS "  Build documentation:         ${XALANC_BUILD_DOCS}")
 message(STATUS "  Thread implementation:       ${thread}")
 message(STATUS "  Transcoder:                  ${transcoder}")
 message(STATUS "  Message Loader:              ${msgloader}")
diff --git a/cmake/XalanDLL.cmake b/cmake/XalanDLL.cmake
index 858c00c0d..5e67d530b 100644
--- a/cmake/XalanDLL.cmake
+++ b/cmake/XalanDLL.cmake
@@ -20,7 +20,7 @@
 # Option for selection of shared or static libraries, exported as
 # cache variable
 
-set(BUILD_SHARED_LIBS ON CACHE BOOL "Build shared libraries")
+#set(BUILD_SHARED_LIBS OFF CACHE BOOL "Build shared libraries")
 
 # Add a D postfix to Debug libraries on Windows
 
diff --git a/src/xalanc/ICUBridge/ICUBridge.cpp b/src/xalanc/ICUBridge/ICUBridge.cpp
index 7310a5286..e05ecd251 100644
--- a/src/xalanc/ICUBridge/ICUBridge.cpp
+++ b/src/xalanc/ICUBridge/ICUBridge.cpp
@@ -101,6 +101,7 @@ ICUBridge::UnicodeStringToXalanDOMString(
             const UnicodeString&    theString,
             XalanDOMString&         theResult)
 {
+    static_assert(sizeof(UChar) == sizeof(XalanDOMChar));
     const int32_t   theLength = theString.length();
 
     if (theStackBufferSize > theLength)
@@ -110,7 +111,7 @@ ICUBridge::UnicodeStringToXalanDOMString(
         // Extract the data...
         theString.extract(0, theLength, theBuffer);
 
-        theResult.assign(theBuffer, theLength);
+        theResult.assign(reinterpret_cast<const XalanDOMChar*>(theBuffer), theLength);
 
         return theResult;
     }
@@ -127,7 +128,7 @@ ICUBridge::UnicodeStringToXalanDOMString(
 
         assert(theLength == int32_t(theBuffer.size()));
 
-        theResult.assign(&theBuffer[0], theLength);
+        theResult.assign(reinterpret_cast<const XalanDOMChar*>(&theBuffer[0]), theLength);
 
         return theResult;
     }
diff --git a/src/xalanc/ICUBridge/ICUBridgeCollationCompareFunctorImpl.cpp b/src/xalanc/ICUBridge/ICUBridgeCollationCompareFunctorImpl.cpp
index 010818b63..26e30caf4 100644
--- a/src/xalanc/ICUBridge/ICUBridgeCollationCompareFunctorImpl.cpp
+++ b/src/xalanc/ICUBridge/ICUBridgeCollationCompareFunctorImpl.cpp
@@ -158,10 +158,11 @@ ICUBridgeCollationCompareFunctorImpl::doCompare(
         theLHSLength == static_cast<int32_t>(theLHSLength) &&
         theRHSLength == static_cast<int32_t>(theRHSLength));
 
+    static_assert(sizeof(char16_t) == sizeof(XalanDOMChar));
     return theCollator.compare(
-                theLHS,
+                reinterpret_cast<const char16_t*>(theLHS),
                 static_cast<int32_t>(theLHSLength),
-                theRHS,
+                reinterpret_cast<const char16_t*>(theRHS),
                 static_cast<int32_t>(theRHSLength));
 }
 
@@ -352,9 +353,9 @@ ICUBridgeCollationCompareFunctorImpl::doCompare(
         theRHSLength == static_cast<int32_t>(theRHSLength));
 
     return theCollator.compare(
-                    theLHS,
+                    reinterpret_cast<const char16_t*>(theLHS),
                     static_cast<int32_t>(theLHSLength),
-                    theRHS,
+                    reinterpret_cast<const char16_t*>(theRHS),
                     static_cast<int32_t>(theRHSLength));
 }
 
